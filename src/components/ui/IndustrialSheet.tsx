/**
 * Global spotlight drawer — preset SlideOverPanel for industrial / ERP flows.
 * Uses pure black panel + 200ms slide animation by default.
 */
export { SlideOverPanel as IndustrialSheet } from '@/components/ui/SlideOverPanel'
