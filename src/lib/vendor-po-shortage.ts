/** Vendor PO flagged after GRN rejection when user chooses replacement (lead-buffer uses `replacementEtaAt`). */
export const PROCUREMENT_SHORTAGE_AWAITING_REPLACEMENT = 'Awaiting_Replacement' as const
